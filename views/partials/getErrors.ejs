<% if(errors.length > 0) { %>
    <div class="filter_errors">
        <ul>
            <% for(let i=0; i<errors.length; i++) { %>
                <li> <%= errors[i].msg %> </li>
            <% } %>
        </ul>
    </div>
<% } %>