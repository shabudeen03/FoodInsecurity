<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"> <!--Ensure all characters/symbols encoded properly to prevent data loss-->
        <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- for flexibility on different screens and sizes-->
        <title>Food Insecurity - Dashboard</title>
        <link rel="stylesheet" type="text/css" href="./stylesheets/index.css">
        <link rel="stylesheet" type="text/css" href="./stylesheets/profile.css"> 
        <link rel="stylesheet" type="text/css" href="/stylesheets/recipes.css">
    </head>

    <body>
        <%- include("partials/header") %>

        <div class="profile-meta">
            <div class="credentials">
                <h1><%= username %></h1>
                <h2><%= firstName %> <%= lastName %></h2>
                <div>Email: <%= email %></div>
                <div>Phone: <%= (phone)? phone : "NOT PROVIDED"  %></div>
            </div>

            <div class="regress-btns">
                <button type="button">
                    <% if(status === "active") { %>
                        <a href="/profile/disable">
                            Disable Account
                        </a>
                    <% } else { %>
                        <a href="/profile/enable">
                            Enable Account
                        </a>
                    <% } %>
                </button>

                <button type="button">
                    <a href="/profile/delete">
                        Delete
                    </a>
                </button>
            </div>
        </div>

        <hr />

        <h2> Your Recipes: </h2>

        <div class="profile-recipes" style="display: flex; text-align: center; justify-content: center; flex-wrap: wrap;">
            <% for(let i=0; i<rows.length; i++) { %>
                <%- include("partials/recipe", { recipe: rows[i] }) %>
            <% } %>
        </div>

        <%- include("partials/footer") %>
    </body>
</html>